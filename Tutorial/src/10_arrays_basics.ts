const a11: number[] = [1, 2, 3];
const a22: Array<number> = [1, 2, 3]; // REACT

const a33: (string | number)[] = [1, 2, 3, 'a', 'b'];

const a44: string[] | number[] = Math.random() > 0.1 ? [1, 2, 3] : ['a', 'b'];
